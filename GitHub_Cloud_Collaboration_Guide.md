# GitHub\_Cloud\_Collaboration\_Guide

## 📌 下载使用git进行团队协作

### 🛠️ 第一阶段：Git 初次配置（本地电脑）

先下载一个GIT FOR WINDOWS

协作者在自己电脑上，任意位置右键点击，选择 `Git Bash Here`（或者打开 CMD/PowerShell），执行以下命令。

**1. 设置用户名和邮箱（必须，否则无法提交）**

bash

```
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub注册邮箱"
```

_解释：这相当于告诉 Git 你是谁，每次提交记录都会记录这个名字。_

**2. 设置默认分支名（统一为 main）**

bash

```
git config --global init.defaultBranch main
```

**3. 设置换行符自动转换（防止 Windows/Mac 修改代码报错）**

bash

```
git config --global core.autocrlf true
```

_解释：防止因为回车换行符不同，导致明明没改代码却显示全部文件被修改。_

***

### 🔑 第二阶段：获取 GitHub 访问凭证（个人访问令牌）

自 2021 年起，GitHub **不再支持直接用密码推送代码**，必须使用 **Personal Access Token (个人访问令牌)**。

1. 协作者打开自己的 GitHub 网页，点击右上角头像 -> **Settings（设置）**。
2. 左侧滚动到底部，点击 **Developer settings（开发者设置）**。
3. 点击 **Personal access tokens（个人访问令牌）** -> **Tokens (classic)**。
4. 点击 **Generate new token (classic)**。
5. 随便起个名字（如 `git-push`），过期时间选 **No expiration（永不过期）** 或 90 天。
6. 下方 **勾选 `repo` 和 `workflow`**（勾选 repo 会自动勾选下面一堆）。
7. 点击底部的绿色 **Generate token** 按钮。
8. **重要：立刻复制生成的这一串字符（类似 `ghp_xxxxxx`）**。关掉页面后就再也看不到了。

***

### 📥 第三阶段：克隆（Clone）项目到本地

假设仓库地址是：`https://github.com/你的用户名/仓库名.git`

**1. 复制仓库地址**

在 GitHub 仓库页面，点击绿色的 **“Code”** 按钮，选择 **HTTPS**，复制地址。

**2. 克隆到本地**

在电脑上选择一个存放代码的文件夹（比如 `D:\projects`），右键打开 `Git Bash Here`（或终端），输入：

bash

```
git clone https://github.com/xixixiCM/Gintoki-socratic-learning.git
```

_解释：这会将远程仓库的所有代码、分支历史完整下载到当前文件夹下的新目录中。_

**3. 进入项目文件夹**

**这一步之后，跳到第七阶段，四到五后面的是一些功能的使用，可以展示不用管**

bash

```
cd Gintoki-socratic-learning
```

***

### 🚀 主要阶段（核心逻辑）      &#x20;

分模块作业的主要逻辑在main主干的基础上复制一个dev分支，用于测试合并代码的稳定性，基于不同模块创建不同的feature分支，每人负责一条或者多条，团队成员通过git进入自己负责的分支，之后的修改上传在本分支下面进行，具体模块改进代码添加部分在自己的电脑本地进行，然后通过git上传到自己负责的feature块，小组成员每天结束之后或者完成一个模块之后一起通过git命令合并到dev中进行测试合并代码是否能正常运行，直至最终版本代码无误，同步至GitHub仓库也就是main主干中

###

###

| **场景**         | **操作目的**                   | **具体 Git 命令（按顺序执行）**                                                                                                                                                                                                                                                               |
| -------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1. 项目初始化**   | 克隆仓库到本地，并切换到 dev           | `<仓库地址>``git switch dev` （如果本地没有，先 `git fetch` 再 `switch`）git fetch origin                    # 1. 刷新云端分支列表（此时会看到 origin/feature/新功能）git branch -r                       # 2. 查看所有远程分支（确认出现了新分支）git switch feature/新功能           # 3. 切换到该分支（Git 会自动创建本地副本并关联） |
| **2. 领取模块任务**  | 基于 `dev` 创建自己的功能分支         | `git switch dev` （确保在最新的 dev 上）​`git pull origin dev` （拉取最新 dev 代码）​`git switch -c feature/你的模块名` （例如：`feature/login-page`）​git push -u origin feature/你负责的模块   # 将该分支上传到云端​git branch -vv  #可以查看自己现在在哪个分支下面                               |
| **3. 日常开发上传**  | 本地改完代码，推送到自己的远程 feature 分支 | `git add .` （暂存所有改动）​`git commit -m "feat: 完成了登录框样式"`​`git push origin feature/你的模块名``​`git status    #查看当前本地有没有未上传的文件                                                                            |
| **4. 合并到测试分支** | 功能完成，合并到 `dev` 进行集成测试      | `git switch dev`​`git pull origin dev` （先把 dev 最新拉下来）​`git merge feature/你的模块名` （把 feature 合并进 dev  若运行无误则下一步）​`git push origin dev` （把合并后的 dev 推到云端）                                                                                                |
| **5. 最终发版上线**  | dev 测试无误，合并到 `main` 主干     | `git switch main`​`git pull origin main`​`git merge dev`​`git push origin main`                                                                                                                                                                      |

###

## 🚫可能出现的疑问

​

### 1、当把dev最新版拉到本地，那这个版本会覆盖掉我写的feature版本吧

​

针对问题 1：拉取 dev 最新版，会覆盖我写的 feature 吗？

**答案：绝对不会！除非你手动执行了合并操作（`git merge`）。**

我把你的工作场景拆成两种情况，你就明白了：

* **情况 A（你正在 feature 分支上干活，执行了 `git pull origin dev`）**：
  * 这个命令的意思是：**“把 dev 的代码拉下来，合并到我当前的 feature 分支里”**。
  * 结果：**dev 的新代码会“融入”你的 feature 分支**，你写的 feature 代码依然存在，两者会合并在一起。如果改到了同一个文件的同一行，会提示冲突，但冲突需要你手动解决，**绝不会自动把你写的代码删掉**。
* **情况 B（你只是切换分支去 dev 看一眼，执行了 `git switch dev` 和 `git pull`）**：
  * 当你 `git switch dev` 时，**你的工作目录会从 feature 切换到 dev**。此时你在 VS Code 里看到的代码变成了 dev 的。
  * 但是！**你 feature 分支上的所有提交，依然安全地躺在你的本地仓库里**，只是暂时“看不见”了。
  * 当你下次 `git switch feature` 切回来时，你之前写的所有代码都会原封不动地出现。

**核心逻辑**：Git 的分支是**独立平行**的。切换分支相当于“换一个房间工作”，而不是“把上一个房间拆了重建”。

### 2、使用git fetch origin dev这个命令下载dev的最新代码，为什么不会合并到当前正在写的feature呢，那这个dev放在了哪里呢

**​**

**答案：`git fetch` 是“只看不拿”，下载到的是“云端的副本”，不影响你当前的工作区。**

我用一个“快递”的类比帮你彻底记住：

* **`git fetch origin dev`**（你的命令）：
  * **动作**：快递员把包裹（dev 的最新代码）放到了你家**门口的快递柜（本地仓库的缓存区）**&#x91CC;。
  * **结果**：你并没有拆开包裹，所以你手上正在做的事（正在写的 feature 代码）完全不受影响。你只是知道“哦，dev 有更新了”。
* **`git pull origin dev`**（你常用的同步）：
  * **动作**：快递员不仅把包裹放到了快递柜，还**帮你拆开并直接摆到了你的桌面上（合并到你当前的分支）**。
  * **结果**：你的工作区发生了变化。

**那么 `fetch` 下来的 dev 具体放在哪里了？**

它放在了 Git 本地的 **“远程跟踪分支”** 里，名字叫 **`origin/dev`**。

* 你本地有两个与 dev 相关的东西：
  1. **你本地的 `dev` 分支**（你 `git switch dev` 时看到的那个，可能比较旧）。
  2. **`origin/dev`**（`fetch` 下载下来的云端快照，只读的，不能直接改）。

**你可以用这个命令查看 `origin/dev` 到底长啥样：**

bash

```
git log origin/dev   # 看看云端 dev 的最新提交记录
git diff origin/dev  # 对比一下 origin/dev 和你当前的代码差在哪
```

如果你想**只看不合并**，但想拿某个特定文件来用，可以用我上次教你的：

bash

```
git checkout origin/dev -- 路径/文件名
```

​

### 3、为什么使用switch命令切换回来之后，feature的文件就在本地出现了，而dev的命令消失了呢，我不需要进行其他操作吗

###

🧠 核心原理：分支不是“复制文件夹”，而是“切换视角”

* **你的电脑硬盘上**（物理层面）：只有一份项目文件（比如 `src`、`backend` 文件夹）。
* **你的 Git 本地仓库里**（管理层面）：存着所有分支的“完整快照”（像一本厚厚的相册，记录了每个时刻所有人的代码）。

当你执行 `git switch dev` 时，Git 实际上做了这两步：

1. **移走当前分支的文件**：把 `feature` 分支对应的所有文件从你的工作目录（桌面/文件夹）中“隐藏”或“移除”。
2. **放入目标分支的文件**：把 `dev` 分支里存好的那套代码，完整解压并放到你的工作目录里。

**所以，你不需要手动去删除、复制或拉取任何东西。`switch` 命令就是用来做这件“原地替换”工作的。**

🧪 验证一下：为什么文件“消失”了，但没丢？

你看到 `dev` 的文件消失，是因为 **`dev` 分支的文件压根不在当前分支的快照里**。

* 你现在站在 `dev` 的视角，当然看不到 `feature` 的文件。
* 但只要你在终端输入 `git switch feature`，**刚才消失的 `feature` 文件会原封不动地全部回来**，而 `dev` 的文件又会“消失”。

**这就像看电影时的“多结局版本”**：你手里只有一个播放器（工作目录），但有不同的光盘（分支）。你插入 `dev` 这张光盘，看到的是结局 A；拿出这张盘，插入 `feature` 这张盘，看到的是结局 B。**你不需要对光盘本身做任何加工，切换盘片就是切换内容。**

🚨 唯一的例外（什么时候需要额外操作？）

**如果你在切换前，没有提交当前的修改**，Git 会有两种反应：

1. **如果修改的文件没有冲突**：Git 会 **“携带”** 这些未提交的修改，一起带到新分支。这时候你会发现新分支里多了你刚才改的内容（这可能不是你想要的）。
2. **如果修改的文件有冲突**：Git 会**报错拦截**，提示 `Please commit your changes or stash them before you switch branches.` 这时候你就需要先执行 `git add .` 和 `git commit -m "保存进度"` 把当前分支的代码存好，再执行 `git switch`。

**所以，你完全不用担心要额外操作。你只需要记住一条铁律：**

> **在 `git switch` 之前，养成习惯先看一眼 `git status`，如果是干净的（没有红色的未提交文件），就直接切换，放心交给 Git 处理。**

​

### 4、就是说如果并没有将修改的项目提交到feature里，直接进行切换，那样文件就会带入到其他分支里，那其他分支里面别的组员还是看不到你没提交的文件是吧，如果提交了使用status还能出现未提交的文件吗

​

问题 1：未提交的文件带到其他分支，别的组员能看到吗？

**答案：绝对看不到！**

* **原因**：未提交的文件（即 `git status` 里显示的红色或绿色文件）**只存在于你本地电脑的硬盘上**，它们还没有被打包成一个“版本包”（Commit），更没有通过 `git push` 传到 GitHub 云端。
* **结果**：其他组员无论怎么 `git pull`，都只能拉到已经被 `push` 到云端的“版本包”，绝对拉不到你本地未提交的草稿。**只要你没执行 `git commit` 和 `git push`，世界上没有任何人能看到你的改动。**

***

问题 2：如果提交了，使用 `git status` 还能出现未提交的文件吗？

**答案：分两种情况。**

情况 A（你提交了所有修改）

如果你执行了 `git add .` 和 `git commit -m "xxx"`，把当前所有修改都保存成了版本，那么此时你的工作区是“干净”的。

此时执行 `git status`，终端会显示：

text

```
nothing to commit, working tree clean
```

**意思是：没有任何未提交的文件。**

情况 B（你提交了部分，或者提交后又改了新内容）

* 如果之前改了 3 个文件，但你只 `git add` 了其中 2 个并 `commit`，那么第 3 个文件依然处于“未提交”状态。此时 `git status` 会显示第 3 个文件是红色的（未暂存）。
* 或者，你在 `commit` 之后，**又继续修改了新的代码**，那么新修改的这些文件也会出现在 `git status` 里。

**核心结论**：`git status` 反映的是“**当前工作目录**”与“**当前分支最后一次提交**”之间的差异。只要你有任何改动没有被 `commit`，`git status` 就会显示出来；如果你的所有改动都已经 `commit` 了，它就会显示 `clean`。

​
